# Default experiment for pretraining and buffer generation

[overwrite.paths]
path_training_data = "data/pre-training-data/training-TBR-circular"
path_replay_buffer = "data/output/replay_buffer.pkl"
path_SAC_model_load = ""
path_SAC_model_save = "data/output/sac_model_final.zip"

[overwrite.training]
training_steps = 10000
pretrain_gradient_steps = 100000
batch_size = 1000
pt_log_interval = 10000
checkpoint_freq = 10000
read_replay_buffer = true

[overwrite.general]
ephem_version = 3.0
