import os

import astro_compass

PROJECT_ROOT = os.path.dirname(os.path.dirname(astro_compass.__file__)) + "/../.."
DATA_ROOT = os.path.join(PROJECT_ROOT, "data")
CONFIG_ROOT = os.path.join(PROJECT_ROOT, "data", "config")
